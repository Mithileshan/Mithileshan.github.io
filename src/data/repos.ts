import type { Project } from '../types'

export const repos: Project[] = [
  {
    slug: 'chatterbox',
    title: 'ChatterBox',
    tagline: 'Room-based real-time chat with WebSocket messaging, typing indicators, and live user presence.',
    category: 'Full-Stack / Real-Time',
    featured: false,
    tech: ['Node.js', 'Express.js', 'Socket.IO', 'Redis', 'HTML5', 'JavaScript'],
    highlights: [
      'Multi-room Socket.IO chat — instant messaging with typing indicators and join/leave notifications',
      'Active user list and timestamped messages — real-time presence for all room participants',
      'Rate limiting (100 req/15min), health endpoint, and Redis-ready for horizontal scaling',
    ],
    links: { github: 'https://github.com/Mithileshan/chatterbox-realtime-chat' },
    caseStudy: {
      problem: [
        'HTTP polling-based chat produces stale messages and wastes bandwidth — users see delays and the server handles unnecessary requests.',
        'Room-based isolation is critical: messages from one room must never leak to another, and user presence must update instantly on join and leave.',
        'A chat system that cannot scale beyond a single process is a dead end — horizontal scaling requires shared state across instances.',
      ],
      solution: [
        'Built a multi-room chat server with Socket.IO over WebSockets — messages are pushed from server to all clients in the same room in real time without polling.',
        'Typing indicators and join/leave notifications are emitted as lightweight events, keeping users aware of room activity without a full message broadcast.',
        'Added an optional Redis adapter that wires pub/sub between server instances — the system degrades gracefully to single-instance mode if Redis is unavailable.',
        'Rate limiting on the HTTP layer (100 req/15min per IP) and a health endpoint for load balancer readiness checks.',
      ],
      architecture: [
        'Client browser → Socket.IO WebSocket connection → Node.js + Express server',
        'on(joinRoom): add user to room namespace, broadcast join notification to room',
        'on(chatMessage): format with timestamp → emit to all sockets in room',
        'on(typing): emit typing indicator to other room members',
        'on(disconnect): remove user from in-memory registry, broadcast leave notification',
        'utils/users.js: in-memory user registry scoped per room — join/leave/query operations',
        'utils/messages.js: message formatting with server-side timestamps for consistency',
        'Redis adapter (@socket.io/redis-adapter): pub/sub across multiple server instances when Redis is present; in-memory fallback when absent',
      ],
      decisions: [
        'Socket.IO over raw WebSockets: provides automatic transport fallback (long-polling, SSE), built-in reconnection logic, and room/namespace abstractions — reduces client complexity significantly',
        'Optional Redis adapter: the system should work without Redis for local development and scale with Redis in production without any code changes — graceful degradation by design',
        'In-memory user state: chat presence is volatile by nature — users rejoin on reconnect, so storing presence in memory (not a database) is the right tradeoff; no persistence needed',
        'Server-side timestamps: timestamps assigned at the server prevent clock skew between clients causing out-of-order message display',
        'Rate limiting on HTTP only: WebSocket events bypass the rate limiter, which is acceptable because Socket.IO connections are already guarded by the initial HTTP handshake',
      ],
      impact: [
        'Sub-100ms message delivery in-process, sub-300ms with Redis pub/sub across instances',
        'Room isolation — Socket.IO namespaces guarantee no cross-room message leakage',
        'Horizontal scaling enabled: adding a Redis adapter enables multiple server instances without any application code changes',
        'Health endpoint ready for load balancer and container orchestration readiness checks',
      ],
      challenges: [
        'Cross-instance message delivery: without shared state, two server instances cannot deliver messages across rooms — solved with the optional Redis pub/sub adapter using @socket.io/redis-adapter',
        'Memory leak prevention: users who disconnect without an explicit leave event must be cleaned up — solved with on(disconnect) handler that removes the user from the in-memory registry',
        'Consistent message ordering: clients processing events asynchronously can receive messages out of order — mitigated with server-assigned timestamps and client-side sort on render',
        'Graceful Redis failure: if Redis goes down in a multi-instance setup, instances lose cross-instance routing — the system falls back to serving only locally connected users without crashing',
      ],
      nextSteps: [
        'MongoDB for chat history persistence — messages survive server restart and page reload',
        'JWT authentication — named identity per user, private direct messages',
        'File and image sharing via S3 presigned upload',
        'Read receipts and message delivery acknowledgement via Socket.IO ack callbacks',
        'WebRTC video/audio for in-room calls',
      ],
    },
  },
  {
    slug: 'streampulse',
    title: 'StreamPulse',
    tagline: 'Real-time clickstream analytics — Kafka events processed through Python consumers into PostgreSQL, served by a TypeScript analytics API.',
    category: 'Data Engineering / Streaming',
    featured: false,
    tech: ['Apache Kafka', 'Python', 'PostgreSQL', 'TypeScript', 'Node.js', 'Docker'],
    highlights: [
      'Producer → Kafka (3 partitions, KRaft) → consumer → dual Postgres tables for raw events and aggregates',
      'Analytics API: top clicks, per-shortcode stats, and trending queries over configurable time windows',
      'Kafka UI for live topic visibility — full stack on Docker Compose',
    ],
    links: { github: 'https://github.com/Mithileshan/streampulse-kafka-clickstream' },
    caseStudy: {
      problem: [
        'URL click events are high-volume and bursty — writing synchronously to a database on every click blocks the response path and risks data loss under load.',
        'Analytics queries (top URLs, trending over time) scan the full events table; running them against the raw event log becomes slow at scale.',
        'A pipeline that cannot replay events is fragile — if the consumer crashes or the aggregation logic changes, there is no way to reprocess historical data.',
      ],
      solution: [
        'Built a producer that publishes click events to a Kafka topic (3 partitions, KRaft mode) at ~5 events/sec — the producer path is non-blocking and decoupled from the database.',
        'Python consumer reads from Kafka and writes to two Postgres tables: click_events (immutable raw log) and click_aggregates (mutable per-shortcode totals via UPSERT).',
        'TypeScript analytics API exposes REST endpoints for top clicks, per-shortcode stats, and trending queries over configurable time windows — all served from pre-aggregated data.',
        'Full stack runs on Docker Compose with Kafka UI for live topic and consumer lag visibility.',
      ],
      architecture: [
        'Python producer (Faker-generated events) → confluent-kafka → click-events topic (3 partitions, keyed by shortCode)',
        'Python consumer group → read from click-events → psycopg2 batch insert',
        'click_events table (Postgres): immutable raw log — shortCode, userId, ip, referrer, userAgent, occurred_at',
        'click_aggregates table (Postgres): ON CONFLICT DO UPDATE — shortCode (PK), total_clicks, last_seen',
        'TypeScript + Express analytics API: GET /stats/:shortCode, GET /stats/top, GET /stats/trending?hours=N',
        'Kafka UI container: live visibility into topic partitions, consumer group lag, and message throughput',
        'Docker Compose: Kafka (KRaft), PostgreSQL, producer, consumer, analytics API, Kafka UI',
      ],
      decisions: [
        'Kafka over direct DB writes: Kafka acts as a durable buffer for traffic bursts — the consumer processes at its own pace without blocking the producer or losing events if the consumer restarts',
        'KRaft mode (no ZooKeeper): eliminates ZooKeeper dependency, reducing the Docker Compose stack from 3 Kafka-related containers to 1; Kafka 3.7+ supports KRaft in production',
        'Dual table strategy (raw + aggregates): click_events is the immutable source of truth for replay; click_aggregates is the fast read path for analytics — each serves a different query pattern',
        'UPSERT on aggregates (ON CONFLICT DO UPDATE): makes the consumer idempotent — if the same event is processed twice (at-least-once delivery), the aggregate total is still correct',
        'TypeScript for analytics API: type-safe request/response contracts with Express; catches schema mismatches at compile time rather than runtime',
      ],
      impact: [
        'Event-to-Kafka latency: <10ms; consumer-to-database latency: <50ms; analytics API query time: <200ms',
        'Full replayability: if aggregation logic changes, reprocess click_events from offset 0 to rebuild aggregates',
        'Consumer crash recovery: Kafka committed offsets mean the consumer resumes from exactly where it left off on restart — no duplicate events, no gaps',
        'Kafka UI gives immediate visibility into consumer lag, partition balance, and throughput without any custom tooling',
      ],
      challenges: [
        'Idempotent aggregation: Kafka at-least-once delivery means the same event can arrive twice — solved with ON CONFLICT DO UPDATE on click_aggregates so duplicate processing produces the same result',
        'Partition key choice: keying by shortCode ensures all clicks for the same URL land in the same partition, preserving ordering for per-URL aggregation without coordination overhead',
        'Consumer group rebalancing: partition assignment shifts when consumers are added or removed — handled by confluent-kafka client library; auto-commit of offsets only after successful DB write',
        'Schema evolution: changing the click event schema requires updating both producer and consumer; Kafka Schema Registry deferred as a next step',
      ],
      nextSteps: [
        'Schema registry for Avro/Protobuf enforcement across producer and consumer versions',
        'Dead letter queue for malformed events that fail consumer validation',
        'Grafana dashboard on top of the analytics API for live click volume visualization',
        'Multiple consumer groups for independent downstream processing (analytics vs alerting)',
        'Kafka Streams or Flink for stateful windowed aggregations directly in the stream',
      ],
    },
  },
  {
    slug: 'urly',
    title: 'URLY',
    tagline: 'URL shortener with click analytics, Redis caching, and JWT auth — built with TypeScript and Hexagonal Architecture.',
    category: 'Full-Stack / SaaS',
    featured: false,
    tech: ['TypeScript', 'Node.js', 'Express.js', 'MongoDB', 'Redis', 'JWT', 'Docker', 'GitHub Actions'],
    highlights: [
      'URL shortening with expiration and per-user ownership — click tracking captures IP, referrer, and user agent',
      'Redis caching with 1h TTL for redirects and 5min for stats — tiered rate limiting across all endpoints',
      'JWT auth with owner-only analytics enforcement and Hexagonal Architecture for clean domain separation',
    ],
    links: { github: 'https://github.com/Mithileshan/URLY-URL-Shortener-Analytics' },
    caseStudy: {
      problem: [
        'Redirect performance is the critical path — every millisecond added to a redirect degrades user experience and hurts SEO metrics.',
        'URL analytics (clicks, referrers, user agents) belong to the URL owner — one user must never be able to read another user\'s stats.',
        'A URL shortener without rate limiting and input validation is a trivial spam amplifier and a denial-of-service vector.',
      ],
      solution: [
        'Built a Redis-first redirect path: every redirect checks cache before hitting MongoDB — 95%+ of requests never touch the database.',
        'Hexagonal Architecture separates domain logic (shorten URL, track click, get stats) from infrastructure (MongoDB, Redis, JWT) — each layer is testable in isolation.',
        'JWT auth with ownership check at the DB layer: stats endpoints verify the requesting user owns the URL before returning data.',
        'Tiered rate limiting: 100 req/15min globally, 10 req/15min on auth endpoints (brute-force protection), 20 req/min on URL creation (bulk spam prevention).',
      ],
      architecture: [
        'POST /api/auth/register|login → bcrypt hash → JWT signed with secret → returned to client',
        'POST /api/url (JWT required) → Zod validation → nanoid 6-char code → store in MongoDB with ownerId and optional expiresAt',
        'GET /:shortCode (public) → check Redis (redirect:shortCode, 1h TTL) → hit: 301 redirect; miss: MongoDB query → cache result → redirect → async click record written',
        'GET /api/url/:code/stats (JWT required) → DB ownership check (requestingUserId === url.ownerId) → aggregated click data from MongoDB → cache in Redis (stats:shortCode, 5min TTL)',
        'Hexagonal layers: Presentation (controllers, middleware, Zod validators) → Domain (entities, use case interfaces) → Data (repository interfaces, application services) → Infrastructure (MongoDB, Redis, JWT, bcrypt, Pino)',
        'Helmet middleware: auto-adds 15 HTTP security headers (CSP, HSTS, X-Frame-Options, X-Content-Type-Options)',
        'Pino structured logging: JSON log lines queryable by log aggregation tools (ELK, CloudWatch Logs)',
      ],
      decisions: [
        'Hexagonal Architecture: domain logic is independent of infrastructure — swapping MongoDB for PostgreSQL or Redis for Memcached requires only infrastructure layer changes, no domain changes',
        'Redis tiered caching (1h redirects, 5min stats): redirects are the hot path so long TTL maximises hit rate; stats are queried less frequently and change on every click so 5min TTL balances freshness and DB load',
        'Nanoid 6-char codes: 36^6 = ~2.2 billion unique codes; probabilistic collision check on insert makes duplicates practically impossible at any realistic scale',
        'Zod validation at the controller boundary: runtime schema validation catches malformed requests before they reach the domain layer — no silent type coercion bugs',
        'MongoDB TTL indexes for URL expiration: Mongo auto-deletes expired documents without a cron job or manual cleanup process',
        'Pino over console.log/Winston: Pino is the fastest Node.js logger (async, minimal overhead on the hot redirect path); structured JSON output is queryable in any log aggregation system',
      ],
      impact: [
        '<50ms redirect latency on Redis cache hit; <200ms on MongoDB fallback',
        '95–99% redirect cache hit rate at 1h TTL for any non-cold-start workload',
        'Owner-only analytics: DB-layer ownership check means no application-layer bypass is possible',
        'Hexagonal separation means every use case is testable with mock infrastructure — no real Redis or MongoDB required in unit tests',
      ],
      challenges: [
        'Cache invalidation on click: every click must invalidate the stats cache so users see up-to-date analytics — handled by deleting stats:shortCode from Redis on every click record write',
        'Ownership check performance: adding a DB ownership check to every stats request adds one extra query — mitigated by indexing ownerId on the urls collection so the check is an O(log n) index scan',
        'Concurrent redirect and click tracking: two requests can arrive for the same short code simultaneously — click writes use MongoDB atomic insertOne; no race condition on the immutable click record',
        'Short code collision: nanoid generates a 6-char code; collision probability is low but non-zero — handled with a DB uniqueness constraint and a retry loop (generate new code on duplicate key error)',
      ],
      nextSteps: [
        'Custom domains: let users map their own domain to a short code (e.g., go.company.com/x)',
        'QR code generation endpoint for each short URL',
        'Geo-analytics: enrich click records with country and city from IP via MaxMind or ipapi',
        'Bulk URL creation API for enterprise use cases',
        'Webhook delivery on click events for downstream integrations',
      ],
    },
  },
  {
    slug: 'cinebook',
    title: 'CineBook',
    tagline: 'Full-stack MERN movie ticket booking platform with seat selection, show scheduling, and an admin dashboard.',
    category: 'Full-Stack / SaaS',
    featured: false,
    tech: ['React', 'Redux', 'Node.js', 'Express.js', 'MongoDB', 'JWT', 'Docker', 'Nginx'],
    highlights: [
      'Seat selection with real-time availability — 5 movies, 4 cinemas, 80 showtimes seeded idempotently',
      'Swagger/OpenAPI 3.0 docs across 50+ endpoints with admin dashboard and booking approval workflows',
      'QR-encoded ticket confirmation with JWT auth and Dockerized deployment behind Nginx',
    ],
    links: { github: 'https://github.com/Mithileshan/movie-ticket-booking-platform' },
    caseStudy: {
      problem: [
        'Ticket booking systems must enforce seat availability in real time — two users selecting the same seat at the same moment cannot both succeed.',
        'Admin workflows (adding movies, scheduling showtimes, approving bookings) need a separate access tier from the public booking flow.',
        'A booking system with 50+ endpoints that lacks API documentation creates a maintenance black hole — every new developer must reverse-engineer the contract.',
      ],
      solution: [
        'Built a MERN stack booking platform with atomic seat reservation: seatsAvailable is decremented in MongoDB atomically on booking creation, preventing overbooking.',
        'JWT-based admin role gives access to movie/cinema/showtime management and a revenue analytics dashboard — regular users only see the booking flow.',
        'Swagger/OpenAPI 3.0 documentation covers all 50+ endpoints with request schemas, response models, and authentication requirements — accessible at /api/docs.',
        'QR-encoded ticket confirmation generated on booking creation; idempotent seed script populates 5 movies, 4 cinemas, and 80 showtimes on first startup.',
      ],
      architecture: [
        'React SPA (port 3000, served by Nginx) → Redux state management for seat selection, booking flow, and admin dashboard',
        'Express API (port 8080): routes → middleware (JWT auth, admin guard) → controllers → Mongoose models → MongoDB',
        'POST /api/reservations: check seatsAvailable > 0 → atomic decrement → create reservation record with QR code → return confirmation',
        'GET /api/showtimes?cinemaId=X&movieId=Y: return available showtimes with seat counts',
        'Admin routes (isAdmin middleware): POST /api/movies, POST /api/cinemas, PUT /api/showtimes/:id, GET /api/admin/revenue',
        'Seed script (SEED_ON_START=true): checks if admin user exists before inserting — safe to restart containers without duplicate data',
        'Docker Compose: MongoDB, Node.js API, React/Nginx; dev and prod configs with environment-specific settings',
      ],
      decisions: [
        'MongoDB for flexible schemas: movie metadata (genres, languages, posters), cinema layouts (2D seat arrays), and reservation records have different shapes — document storage fits naturally without schema migrations',
        'Redux for state management: seat selection state (which seats are selected, availability grid) is complex enough to need a global store — prop drilling through 4+ component levels would be unmanageable',
        'Swagger-first documentation: 50+ endpoints without docs creates an unmaintainable API — Swagger is generated from JSDoc annotations on route handlers, keeping docs always in sync with the code',
        'Idempotent seed script: checks existence before insert so the script is safe to run on every container start without duplicating demo data',
        'Nginx for React build: serves the static React build with gzip compression and proper cache headers; also proxies /api to the Node.js service in production',
        'QR codes on ticket generation: QR-encoded reservation ID allows future verification (scan to confirm) without additional API calls',
      ],
      impact: [
        '<200ms seat reservation latency with MongoDB atomic operations on indexed showtimes',
        'Overbooking structurally prevented: seatsAvailable check and decrement are atomic — concurrent bookings for the last seat cannot both succeed',
        '80 showtimes across 5 movies and 4 cinemas seeded in <5 seconds on startup',
        'Swagger UI at /api/docs: all endpoints self-documented with examples — zero onboarding friction for any new developer',
      ],
      challenges: [
        'Concurrent seat booking: two users can select the same seat simultaneously — solved with MongoDB atomic findOneAndUpdate on seatsAvailable with a $gt: 0 condition; the second write fails cleanly',
        'Admin authorization: admin-only routes must reject regular JWT tokens even if the token is valid — solved with a separate isAdmin middleware that checks the role field in the JWT payload',
        'Idempotent seeding: restarting the container without idempotent seeding creates duplicate movies and cinemas — solved by checking for admin user existence as the seed guard condition',
        'React SPA routing with Nginx: deep links (e.g., /movies/123) return 404 from Nginx because the route is client-side — solved with try_files $uri /index.html in Nginx config',
      ],
      nextSteps: [
        'Real-time seat availability updates via WebSockets — seats locked by other users disappear from the grid without refresh',
        'Stripe payment integration with webhook confirmation before finalizing reservation',
        'Email confirmation with PDF ticket and QR code attachment',
        'Recommendation engine: suggest movies based on booking history',
        'Multi-language and multi-currency support for international markets',
      ],
    },
  },
  {
    slug: 'csv-etl',
    title: 'CSV → Postgres ETL',
    tagline: 'Production-grade CSV→PostgreSQL ETL pipeline with schema validation, quality gates, and idempotent incremental loads.',
    category: 'Data Engineering',
    featured: false,
    tech: ['Python', 'PostgreSQL', 'SQLAlchemy', 'Docker', 'pytest', 'GitHub Actions'],
    highlights: [
      'Strict validation gates — email, country codes, multi-format dates — with 5% invalid ratio threshold',
      'Idempotent upsert via PostgreSQL ON CONFLICT with SHA256 file hashing for deduplication',
      'Full audit trail: etl_runs and etl_bad_rows tables capturing error counts and load metadata',
    ],
    links: { github: 'https://github.com/Mithileshan/csv-to-postgres-etl-pipeline' },
    caseStudy: {
      problem: [
        'Loading raw CSVs directly into a production database without validation corrupts data integrity — one bad row with a malformed email or invalid country code can break downstream applications.',
        'Re-running an ETL job without deduplication logic creates duplicate records — a 10K-row CSV run twice produces 20K rows.',
        'Without an audit log, diagnosing which batch introduced a data quality problem requires inspecting the entire table history.',
      ],
      solution: [
        'Built a 6-phase ETL pipeline: Extract → Check Incremental (SHA256 file hash) → Validate → Quality Gates → Transform → Load (UPSERT) → Audit.',
        'SHA256 hash of the CSV file is checked against etl_runs before processing — if the file was already loaded successfully, the pipeline exits immediately with no duplicate writes.',
        'Quality gates fail the entire batch if invalid row ratio exceeds 5% — preventing a bad file from partially corrupting the database.',
        'Every run writes to etl_runs (status, row counts, duration) and etl_bad_rows (rejected rows with specific error reasons) — full audit trail for every load.',
      ],
      architecture: [
        'Phase 1 — Extract: read CSV file → compute SHA256 hash of file contents',
        'Phase 2 — Check incremental: SELECT FROM etl_runs WHERE file_hash=X AND status=SUCCESS → skip if found',
        'Phase 3 — Validate: per-row validation (email regex, ISO 3166-1 country codes, name length 2–255 chars, date format detection across 5 formats) → separate valid and invalid rows',
        'Phase 4 — Quality gates: Gate 1: invalid row ratio < MAX_INVALID_RATIO (default 5%); Gate 2: duplicate emails ≤ MAX_DUPLICATE_EMAILS → fail batch on any gate violation',
        'Phase 5 — Transform: map CSV schema to target schema (rename columns, normalize date formats)',
        'Phase 6 — Load: INSERT INTO staging_users (raw copy) → UPSERT INTO users ON CONFLICT (email) DO UPDATE SET ...',
        'Phase 7 — Audit: INSERT INTO etl_runs (file_hash, source_file, rows_processed, rows_loaded, rows_failed, status, duration) → INSERT INTO etl_bad_rows (etl_run_id, raw_row, error_reason)',
      ],
      decisions: [
        'SHA256 file hashing for incremental detection: hashing the entire file catches any content change (even a single character) — more reliable than file modification timestamps which can be wrong on copy',
        'Fail-fast quality gates before load: validating all rows first and aborting on threshold violation prevents partial loads that would be harder to roll back than a clean abort',
        'Separate staging_users table: keeps the raw CSV data alongside the cleaned users table — enables comparing what was ingested vs what passed validation without looking at etl_bad_rows',
        'UPSERT (ON CONFLICT DO UPDATE): makes every load idempotent — the same CSV can be loaded multiple times (after the hash check is bypassed for testing) with the same result',
        'Configurable thresholds via environment variables (MAX_INVALID_RATIO, MAX_DUPLICATE_EMAILS): different environments (dev, staging, prod) can enforce different quality standards without code changes',
        'Parameterized queries via SQLAlchemy: no string interpolation in SQL — SQL injection is structurally impossible regardless of CSV content',
      ],
      impact: [
        'Processing speed: ~1000 rows/sec through the full validate → transform → load pipeline',
        'Zero duplicate records: SHA256 watermarking ensures any file processed successfully is never loaded again',
        'Bad rows quarantined: etl_bad_rows captures every rejected row with the specific validation error — data quality problems are diagnosable without inspecting source files',
        '95%+ test coverage via pytest: every validation rule, quality gate, and upsert behavior has an automated test — regressions are caught in CI before deployment',
      ],
      challenges: [
        'Multi-format date parsing: source CSVs arrive with dates in YYYY-MM-DD, DD/MM/YYYY, MM-DD-YYYY, and datetime formats — solved with an ordered format trial list that tries each format and records the parse error if all fail',
        'Email deduplication across batch and database: the same email can appear twice in one CSV and also exist in the users table — solved with a pre-load dedupe pass within the batch followed by ON CONFLICT handling at the DB layer',
        'Quality gate threshold calibration: too strict (0% invalid) blocks legitimate files with occasional bad rows; too loose (50%) lets garbage through — defaulted to 5% as a reasonable production starting point with environment-variable override',
        'Atomic audit writes: if the server crashes after loading rows but before writing the etl_runs record, the file hash is not recorded and the next run reprocesses the file — mitigated by writing the etl_runs record inside the same transaction as the final UPSERT batch',
      ],
      nextSteps: [
        'S3 source support: poll an S3 bucket for new CSV files, process them automatically on upload',
        'Async parallel processing: split large CSVs into chunks and process chunks in parallel with worker processes',
        'Apache Airflow DAG for scheduled ingestion with retry and alerting',
        'Slack/email alert on quality gate failure — notify data team immediately instead of waiting for next morning audit review',
        'Configurable schema mapping: define target column mapping in YAML so the pipeline works with any CSV schema without code changes',
      ],
    },
  },
  {
    slug: 'logpulse',
    title: 'LogPulse',
    tagline: 'Centralized log aggregation and visualization using Loki, Promtail, and Grafana — Dockerized observability with dashboards and alert rules.',
    category: 'SRE / Observability',
    featured: false,
    tech: ['Loki', 'Promtail', 'Grafana', 'Python', 'Docker Compose'],
    highlights: [
      'Centralized log aggregation with 7-day retention and automatic label extraction (service, level, container)',
      'Pre-built Grafana dashboard with log volume, error count, and error rate panels',
      'Alert rule firing above 10 errors/5min — failure simulation mode for end-to-end observability testing',
    ],
    links: { github: 'https://github.com/Mithileshan/logpulse-observability' },
    caseStudy: {
      problem: [
        'Distributed services write logs to separate container stdout streams — debugging a cross-service incident means SSHing into multiple hosts and manually grepping log files.',
        'ELK stack (Elasticsearch + Logstash + Kibana) requires significant resources and operational complexity — impractical for teams that want observability without dedicated infra engineering.',
        'Without alert rules on error spikes, incidents are discovered by users rather than the engineering team — mean time to detection is hours instead of minutes.',
      ],
      solution: [
        'Built a Loki-based log aggregation stack: Promtail automatically discovers Docker containers via the Docker socket, extracts labels (service, level, container), and ships logs to Loki without any per-service configuration.',
        'Grafana dashboards with pre-built LogQL queries visualize log volume, error count, and error rate in real time.',
        'Alert rule fires when error count exceeds 10 in a 5-minute window — tested with a failure simulation mode that forces 80% error rate on the Python log generator.',
        'Full stack runs on Docker Compose with auto-provisioned Grafana datasource — operational immediately after docker compose up with no manual configuration.',
      ],
      architecture: [
        'Python log generator: structured JSON logs (INFO/ERROR/WARNING) with configurable rates; FAILURE_MODE=true triggers 80% ERROR rate for alert testing',
        'Docker container stdout → Promtail container (Docker socket mount)',
        'Promtail: discovers containers automatically → extracts JSON fields (level, service_name) → attaches labels (service, level, container, job) → ships log streams to Loki HTTP API',
        'Loki 3.4.2: indexes only labels (not log line content) → stores log chunks on filesystem with 7-day retention → exposes LogQL query API to Grafana',
        'Grafana 11.5.2: datasource auto-provisioned from grafana/provisioning/datasources → dashboards loaded from grafana/provisioning/dashboards → alert rules evaluated every 1 minute',
        'Dashboard panels: log volume {count_over_time({service_name="demo-service"}[1m])}, error count {count_over_time({level="ERROR"}[1m])}, error rate %',
        'Alert rule: sum(count_over_time({level="ERROR"}[5m])) > 10 → pending 1 minute → firing state',
      ],
      decisions: [
        'Loki over ELK: Loki indexes only labels (service, level, container) rather than full log text — dramatically lower memory and storage footprint; log line search uses grep-style matching at query time rather than inverted index',
        'Promtail Docker socket discovery: no per-service log shipper configuration needed — Promtail discovers every container automatically and maps container labels to Loki stream labels',
        'Structured JSON logging: each log field is queryable in LogQL filter expressions (| json | level="ERROR") — unstructured logs require regex parsing which is slower and more fragile',
        '7-day retention: balances storage cost (estimated ~5GB for typical log volume) with typical incident investigation window; configurable in loki-config.yaml',
        'Auto-provisioned Grafana datasource and dashboards: eliminates the manual setup step — the stack is fully operational after a single docker compose up command, making it immediately demonstrable',
        'Failure simulation mode (FAILURE_MODE=true): allows testing the alert pipeline end-to-end without waiting for a real error spike — critical for validating that the alert actually fires before relying on it in production',
      ],
      impact: [
        'Log ingestion: 16 MB/s sustained throughput (24 MB/s burst) on single-node Loki',
        'Query latency: <1 second for recent log queries within the 7-day retention window',
        'Alert detection time: error spike detectable within ~2 minutes of onset (1-minute evaluation interval + 1-minute pending period)',
        'Zero per-service configuration: Promtail Docker discovery means any new service added to Docker Compose is automatically picked up and its logs shipped to Loki',
      ],
      challenges: [
        'Label cardinality: attaching high-cardinality labels (request IDs, user IDs) to log streams causes Loki index bloat — solved by limiting labels to low-cardinality fields (service, level, container) and using LogQL filter expressions for high-cardinality search',
        'JSON parsing in Promtail: logs must be valid JSON for field extraction; malformed JSON lines fall back to raw string storage without label enrichment — mitigated by validating log output format in the Python generator',
        'Alert evaluation lag: Grafana alert rules evaluate on a 1-minute interval with a 1-minute pending period — a spike that resolves in <2 minutes may not fire; acceptable for this use case but would need tuning for stricter SLAs',
        'Filesystem storage for Loki: data is stored in a Docker volume and lost if the volume is deleted — production deployment requires object storage (S3, GCS) backend for Loki',
      ],
      nextSteps: [
        'S3 or GCS backend for Loki storage — persistent, scalable, cost-effective for long retention',
        'Multi-service pipeline: add additional Python services with different failure patterns to demonstrate cross-service correlation',
        'Prometheus metrics integration: scrape container metrics (CPU, memory, request latency) alongside logs for full golden signals observability',
        'Grafana authentication hardening — replace default admin/admin123 with OAuth or LDAP',
        'Loki auth enabled (auth_enabled: true) with per-tenant log isolation for multi-team deployments',
      ],
    },
  },
]
