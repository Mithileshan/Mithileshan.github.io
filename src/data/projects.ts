import type { Project } from '../types'

export const projects: Project[] = [
  {
    slug: 'veridoc',
    title: 'VeriDoc',
    tagline: 'A developer-first B2B KYC platform for Indian document verification — embeddable in any product with one npm install.',
    category: 'B2B SaaS / Identity Verification',
    featured: true,
    status: 'In Development',
    tech: ['FastAPI', 'Celery', 'React', 'Claude Vision', 'AWS Rekognition', 'AWS S3', 'MongoDB Atlas', 'Supabase', 'Redis', 'Python', 'TypeScript'],
    highlights: [
      'FastAPI + Celery async pipeline runs Claude Vision extraction, tamper detection, and AWS Rekognition face match in parallel per verification job',
      'Images upload directly to AWS S3 via presigned URLs — never touching the application server — then a signed HMAC-SHA256 webhook fires to the tenant',
      'Cross-tenant isolation enforced at the Supabase RLS layer via JWT org_id claim, not the application layer',
    ],
    links: {
      github: 'https://github.com/Mithileshan/veridoc',
    },
    caseStudy: {
      problem: [
        "India's identity verification market is dominated by enterprise-only platforms (Signzy, HyperVerge, IDfy) with long integration cycles and opaque pricing.",
        'Mid-market gig platforms, rental marketplaces, and edtech products need a self-serve, embeddable KYC API they can test in a sandbox and go live without a sales call.',
        'VeriDoc is built specifically for that gap — a developer-first B2B KYC platform purpose-built for Indian PAN and Passport verification.',
      ],
      solution: [
        'Tenants sign up on the VeriDoc dashboard, generate a scoped API key, and call POST /v1/sdk/session from their backend to receive a short-lived session token (30-minute expiry) — the API key never touches the browser.',
        'The tenant drops <VeriDocCapture sessionToken={...} /> into their React app. End users complete a consent screen, select PAN or Passport, and capture via live camera with real-time quality checks on every frame.',
        'Images upload directly to AWS S3 via presigned URLs — never passing through the application server. A Celery async pipeline then runs Claude Vision extraction, tamper detection, and AWS Rekognition face match in parallel.',
        'A weighted confidence score drives the verification decision (verified / tenant_review_required / rejected), and a HMAC-SHA256 signed webhook fires to the tenant endpoint with retry schedule: 1m, 5m, 30m, 2h, 12h.',
      ],
      architecture: [
        'Tenant backend → POST /v1/sdk/session (API key auth) → session token returned; API key never touches browser',
        'React SDK: consent screen → document type selection → live camera with per-frame quality checks (Laplacian variance blur, overexposed pixel glare ratio, bounding-box coverage, 1280×720 min resolution) → presigned S3 upload',
        'POST /v1/verify → FastAPI validates + scopes to org_id via middleware → job_id returned immediately (HTTP 202) → client polls GET /v1/verify/{job_id} every 3s',
        'Celery chord: Task A (Claude Vision extraction → Pydantic schema validation, PAN regex / Passport MRZ checksum), Task B (Claude Vision tamper risk → font inconsistency, pixel artifacts, edge distortion, JPEG compression flags), Task C (AWS Rekognition CompareFaces, threshold 80.0) — all three parallel',
        'Scoring engine: extraction 25% + tamper risk 40% + face match 35% → decision with hard overrides (id_number regex fail, face similarity < 50, expired document → force rejected)',
        'Signed HMAC-SHA256 webhook: X-VeriDoc-Signature, X-VeriDoc-Timestamp, X-VeriDoc-Event-Id headers; retry schedule 1m → 5m → 30m → 2h → 12h (max 5 attempts)',
        'MongoDB Atlas: verification records with variable-schema extraction fields (PAN fields differ from Passport fields — flexible documents fit MongoDB)',
        'Supabase Postgres: tenant identity, append-only audit_events (no UPDATE/DELETE via Postgres rules), RLS on all tables via auth.jwt() ->> org_id',
        'Data lifecycle: S3 images deleted after 30 days via lifecycle policy; consent records retained with full audit trail',
      ],
      decisions: [
        'Two-database design (MongoDB Atlas + Supabase Postgres): extraction fields differ by document type — PAN extracts name/father_name/dob/pan_number, Passport extracts name/dob/nationality/passport_number/expiry/MRZ. Variable-schema documents fit MongoDB. Tenant identity and audit log need relational structure and RLS enforcement — Postgres provides both natively.',
        'Direct S3 presigned upload over server-proxied upload: document images never pass through the application server — reduces attack surface, liability, and bandwidth cost. 5-minute TTL with server-tracked upload slots prevents replay.',
        'Async pipeline with immediate job_id (HTTP 202) over synchronous response: AI pipeline takes 15–45 seconds — a synchronous HTTP response would time out. Client gets job_id immediately and polls or receives webhook.',
        'RLS at the database layer, not only the application layer: cross-tenant isolation for Postgres tables enforced via Supabase RLS policies using auth.jwt() ->> \'org_id\' — not just checked in application code. Eliminates an entire class of data-leak bugs.',
        'HMAC-SHA256 signed webhooks with timestamp tolerance: tenants verify payload integrity before processing. Timestamp validated within a 5-minute window to prevent replay attacks.',
        'Client-side quality checks before upload: blur, glare, coverage, and resolution checks run on every camera frame before capture. Raises the cost of submitting low-quality or substituted images at zero server cost.',
      ],
      impact: [
        'Self-serve B2B KYC: tenant goes from API key to live document verification with one npm install and one POST /v1/sdk/session call — no sales cycle',
        'Three AI verification signals (extraction, tamper detection, face match) run in parallel via Celery chord without sequential bottleneck',
        'Cross-tenant data isolation enforced at the database layer (Postgres RLS) rather than only in application code — eliminates an architectural class of data-leak bugs',
        'Document images never touch the application server — attack surface, liability, and bandwidth cost structurally reduced by design',
      ],
      challenges: [
        'Presigned URL security: preventing reuse and replay attacks — solved with server-tracked slot expiry (5-minute TTL) and rejection of any duplicate or expired upload attempt at the slot validation layer',
        'Parallel AI pipeline orchestration: three independent Celery tasks must all complete before scoring — handled with Celery chord, per-task timeout to prevent indefinite blocking on API failures',
        'Cross-tenant isolation in MongoDB: Postgres RLS handles relational isolation declaratively, but MongoDB queries must be scoped at the repository layer — enforced with org_id-scoped repository functions and CI cross-tenant isolation tests',
        'Client-side capture quality without native device APIs: Laplacian variance for blur, overexposed pixel ratio for glare, and bounding-box ratio for coverage — all computed on canvas from live camera frames before capture is allowed',
      ],
      nextSteps: [
        'Aadhaar OTP verification integration via UIDAI-compliant APIs',
        'Webhook delivery dashboard for tenants — per-event status, retry history, and manual replay',
        'Sandbox mode with synthetic documents for zero-friction developer testing',
        'Usage-based billing and quota enforcement per tenant API key',
        'Audit export in PDF/CSV for compliance reporting',
      ],
    },
  },
  {
    slug: 'multi-agent-planner',
    title: 'Multi-Agent Document Planning Platform',
    tagline: 'A document-grounded multi-agent reasoning system with a bounded LangGraph state machine and hard cost limits enforced before every model call.',
    category: 'AI Systems / Multi-Agent',
    featured: true,
    status: 'In Development',
    tech: ['LangGraph', 'FastAPI', 'Next.js', 'PostgreSQL', 'pgvector', 'Redis', 'Ollama', 'SSE', 'Python', 'TypeScript', 'Tailwind', 'shadcn/ui'],
    highlights: [
      'Planner, two critic agents (logic + evidence), and a judge run through a bounded revision loop — MAX_REVISION_ROUNDS=1, hard stop on failure',
      'Every agent event streams live to Next.js via SSE; cost guard estimates worst-case token cost and rejects calls that exceed the $0.05/run budget',
      'Defaults to local Ollama inference at $0/month with hosted model fallback only when ALLOW_HOSTED_MODELS=true',
    ],
    links: {
      github: 'https://github.com/Mithileshan/multi-agent-planner',
    },
    caseStudy: {
      problem: [
        'Most LLM-powered Q&A tools return a single model response with no reasoning trace — no visibility into how the answer was formed or what evidence supports it.',
        'Open-ended agent loops hallucinate, go off-topic, and cost unpredictable amounts per run — there is no structural enforcement preventing runaway behavior.',
        'This platform solves both: agent responses are grounded in uploaded documents, the workflow is a deterministic state machine with a bounded revision loop, and every model call is cost-checked before execution.',
      ],
      solution: [
        'User uploads documents, selects them, and submits a query. Backend retrieves top-k relevant chunks from Postgres + pgvector via cosine similarity (capped at 12). All agent calls are grounded in retrieved chunks — no agent responds from memory alone.',
        'LangGraph state machine runs: planner creates execution plan → two critics review in parallel → judge approves or requests one revision → user approves the plan at a hard gate → executor runs approved steps sequentially.',
        'Every agent event streams to the Next.js frontend via SSE. Users watch the plan form, critics review it, the judge decide, and each step complete in real time with citations visible at every step.',
        'Cost guard estimates worst-case token cost before every model call. If it exceeds the per-run budget ($0.05 default), the call is rejected — not warned, rejected. Default inference via local Ollama at $0/month.',
      ],
      architecture: [
        'Document ingestion: parse → chunk → embed locally via Ollama (nomic-embed-text / bge-base) → store chunks + embeddings in Postgres with pgvector',
        'Query path: user query → pgvector cosine similarity retrieval (top-k, capped at MAX_DOCUMENT_CHUNKS_PER_QUERY=12) → chunks become grounding context for all agent calls',
        'Cost guard: estimate input tokens → estimate max output tokens → compute worst-case cost → compare against remaining run budget → reject if over limit → log estimate → execute → log actual usage',
        'LangGraph graph: planner node → [logic critic, evidence critic] in parallel via asyncio.gather → judge node (approve / revise / reject with one bounded revision) → user approval gate (hard stop on plan.status) → executor node (sequential step execution)',
        'SSE event stream on GET /runs/{run_id}/events: run.started → document.retrieval.completed → planner.started → planner.delta → planner.completed → critic.started → critic.delta → critic.completed → judge.completed → plan.awaiting_user_approval → executor.step.started → executor.step.delta → executor.step.completed → final.completed → run.completed',
        'Persistence: runs, agent_messages, plans, plan_steps, step_outputs, citations — all in Postgres; Redis for async job queuing',
        'Trace log: every run produces full audit — agent calls, models used, input/output token counts, latency, cost, critic votes, judge decisions, plan versions, step outputs with chunk citations',
      ],
      decisions: [
        'LangGraph state machine over free-form agents: the workflow is a deterministic graph with explicit nodes (planner → critics → judge → executor) and explicit transition rules. Loops are bounded (MAX_REVISION_ROUNDS=1). No emergent agent behavior — every transition is a controlled state change.',
        'SSE over WebSockets: first version only needs one-way streaming from server to browser — agent deltas flowing out, not bidirectional editing. SSE keeps the backend stateless and avoids connection state management. WebSockets deferred until multi-user collaboration is needed.',
        'Postgres + pgvector over a dedicated vector database: one database stores documents, chunks, runs, agent logs, plans, step outputs, citations, and vectors. No Pinecone, no Qdrant, no separate infra. Sufficient for document scale this system targets and removes a paid dependency entirely.',
        'Local-first model routing (Ollama default): default model is local Ollama (qwen2.5-coder, llama3.1, mistral). Hosted models (Groq, OpenAI) activate only when ALLOW_HOSTED_MODELS=true. Development cost is exactly $0/month and the architecture is not coupled to any paid provider.',
        'Hard cost limits before every model call: cost guard estimates worst-case cost before execution. If a call would exceed the run budget, it is rejected at the infrastructure layer — not a warning, a rejection.',
        'Bounded revision loop with hard stop: one revision round maximum (MAX_REVISION_ROUNDS=1). If the revised plan still fails judge approval, the run fails with a clear reason. Open-ended revision loops are architecturally prevented, not just discouraged.',
      ],
      impact: [
        'Document-grounded responses: no agent call can respond from memory — all outputs must cite retrieved chunks from uploaded documents',
        'Zero hallucination-prone open loops: deterministic LangGraph state machine with explicit transition rules and bounded revision (MAX_REVISION_ROUNDS=1)',
        '$0/month development cost: full system runs locally via Ollama with no paid API dependencies unless ALLOW_HOSTED_MODELS=true',
        'Full reasoning transparency: every agent call, token count, cost, critic comment, and judge decision is logged and inspectable in the trace',
      ],
      challenges: [
        'Streaming LangGraph state transitions to SSE: LangGraph emits events as the graph runs, but FastAPI SSE needs to forward them as they arrive — solved with an async generator that wraps the LangGraph streaming interface and yields formatted event strings to StreamingResponse',
        'Cost estimation accuracy: estimating token count before execution requires a fast tokenizer and conservative output token assumption — implemented with tiktoken for input estimation and configurable max_tokens_per_call cap as the worst-case output bound',
        'User approval gate in an async pipeline: execution must hard-stop after the judge approves until the user explicitly approves the plan — implemented as a database state check on plan.status with the executor refusing to start until status transitions to user_approved',
        'Parallel critic synthesis: two critics run concurrently but the judge needs both outputs before deciding — handled with asyncio.gather for critic execution, then judge receives both results as a structured prompt',
      ],
      nextSteps: [
        'Multi-user workspace collaboration with real-time co-editing of plan steps',
        'Architecture diagram generation from agent outputs via Mermaid or D3',
        'Citation highlighting in the source document viewer — click a step output to jump to the source chunk',
        'Custom agent definitions per workspace — user-defined critic personas and judge scoring rubrics',
        'Export run trace as PDF report with full citation chain',
      ],
    },
  },
  {
    slug: 'stockpulse-etl',
    title: 'StockPulse ETL',
    tagline: 'Real-time and batch stock data ETL pipeline with Redpanda streaming, dual Postgres tables, and a FastAPI analytics layer.',
    category: 'Data Engineering / Streaming',
    featured: true,
    tech: ['Redpanda', 'Kafka', 'PostgreSQL', 'FastAPI', 'Python', 'Docker', 'Docker Compose'],
    highlights: [
      'Tick events produced to Redpanda (3 partitions) → consumed by Python workers → aggregated into OHLCV candles → written to raw + aggregate Postgres tables',
      'Batch backfill path uses SHA256 file hashing for idempotent upserts — no duplicate records regardless of rerun count',
      'FastAPI analytics endpoints serve per-symbol and time-range OHLCV queries directly from the aggregate table',
    ],
    links: {
      github: 'https://github.com/Mithileshan/stockpulse-batch-realtime-etl',
    },
    caseStudy: {
      problem: [
        'Financial data systems need both real-time ingestion (live ticks) and historical batch processing (backfills) — rarely solved cleanly with one pipeline design.',
        'Raw tick data is noisy and high-volume; it needs to be aggregated into OHLCV bars for analytics to be meaningful.',
        'Building a reliable streaming pipeline requires careful attention to ordering guarantees, idempotency, and consumer group design.',
      ],
      solution: [
        'Built a hybrid ETL with two distinct pipelines: a streaming path for real-time ticks and a batch path for historical backfills — both writing to the same Postgres schema.',
        'Streaming path: producer publishes tick events to Redpanda, consumer aggregates into 1-minute OHLCV bars, writes downstream.',
        'Batch path: historical data is loaded, transformed, and inserted with deduplication via ON CONFLICT logic.',
        'FastAPI layer exposes analytics queries: OHLCV by symbol, time-range queries, and aggregated market statistics.',
        'Full stack runs on Docker Compose for a completely reproducible local development environment.',
      ],
      architecture: [
        'Producer: publishes stock tick events to Redpanda topic stock.ticks.v1 (3 partitions, keyed by ticker symbol)',
        'Consumer: reads from stock.ticks.v1, aggregates into 1-minute OHLCV bars, publishes to stock.bars.1m.v1',
        'Downstream consumer: reads bars topic and upserts into Postgres with idempotency keys (symbol + timestamp)',
        'Batch loader: reads historical OHLCV data, deduplicates via SHA256 file hashing, and bulk-inserts into same Postgres schema',
        'FastAPI: exposes typed REST endpoints for analytics queries against Postgres with Pydantic response models',
        'Docker Compose: orchestrates Redpanda, Redpanda Console UI, PostgreSQL, and all service containers',
      ],
      decisions: [
        'Redpanda over Kafka: Kafka-compatible protocol with simpler operations (no ZooKeeper dependency), faster startup — ideal for local development and portfolio demonstrations without sacrificing API compatibility',
        'At-least-once delivery + idempotent upserts over exactly-once semantics: simpler implementation, deduplication handled at the Postgres layer with ON CONFLICT DO NOTHING on composite unique keys',
        'Partition by ticker symbol: guarantees ordering of all ticks for a given stock within a single partition, making aggregation deterministic',
        'FastAPI over Flask: async-native with automatic OpenAPI docs generation and type-safe Pydantic models — better long-term foundation for a data API',
        'Separate topics for raw ticks and aggregated bars: clean separation of concerns, allows independent scaling of each consumer stage',
      ],
      impact: [
        'Throughput: TBD messages/sec (local Redpanda single-node benchmark)',
        'Tick-to-table latency: TBD ms (end-to-end streaming pipeline measurement)',
        'Batch backfill rate: TBD rows/sec',
        'Storage footprint: TBD rows/day for TBD symbols tracked concurrently',
      ],
      challenges: [
        'Consumer lag during tick bursts: high-frequency simulation caused consumer lag — addressed by tuning max.poll.records and commit interval to batch more aggressively',
        'Deduplication at scale: batch re-runs produce duplicate rows — solved with ON CONFLICT DO NOTHING on a composite unique key (symbol, bar_timestamp), making all batch runs fully idempotent',
        'Out-of-order tick arrival: ticks arriving slightly out of sequence from the producer — handled by buffering within a time window before finalizing OHLCV aggregation',
        'Local resource contention: running Redpanda + Postgres + multiple consumers on one machine — tuned Docker memory limits and added container health checks to prevent cascading startup failures',
      ],
      nextSteps: [
        'Schema registry (Redpanda Schema Registry) for Avro/Protobuf schema enforcement across producers and consumers',
        'Feature store for pre-computed technical indicators: RSI, MACD, moving averages — computed on the bars stream',
        'Materialized views in Postgres for sub-millisecond dashboard query response times',
        'Redis caching layer for hot symbol queries that hit the FastAPI tier repeatedly',
        'Alerting pipeline: price spike detection, anomaly alerts, dead-letter queue for failed consumer messages',
        'Full observability with Prometheus metrics from Redpanda + consumers, visualized in Grafana',
      ],
    },
  },
  {
    slug: 'mana-ai',
    title: 'Mana.ai',
    tagline: 'AI meeting assistant that transcribes audio, extracts action items, and creates Trello cards — triggered by one upload.',
    category: 'AI Product / Hackathon',
    featured: true,
    award: 'Luddy Hackathon 4th Edition — 2nd Place',
    role: 'Led the backend pipeline orchestration — designed and implemented the Flask API that coordinates the transcription, summarization, and emotion analysis services. Handled Supabase data schema design and migration, built the Trello integration for automated task syncing, and implemented retry logic and error handling across all service boundaries.',
    tech: ['AssemblyAI', 'Groq', 'Hume AI', 'Supabase', 'Flask', 'Trello API', 'Python'],
    highlights: [
      'Audio transcribed via AssemblyAI with speaker diarization → Groq LLM extracts structured summary and action items → Hume AI analyzes emotion tone in parallel',
      'Action items automatically created as Trello cards via API — zero manual copy-paste from meeting to task board',
      'Full working demo shipped in under 24 hours at hackathon',
    ],
    links: {
      github: 'https://github.com/Mithileshan/mana-ai',
    },
    caseStudy: {
      problem: [
        'Meetings generate dense, unstructured information that is rarely captured well — action items get lost, decisions are forgotten.',
        'Manual note-taking is distracting and inconsistent; important qualitative context like speaker tone is never captured.',
        'Even when notes exist, they are rarely converted into actionable tasks in project management tools — it requires manual effort.',
      ],
      solution: [
        'Built an AI meeting assistant that takes raw audio and produces: full transcript, structured summary, action item list, and emotion insights.',
        'Transcription via AssemblyAI with speaker diarization to attribute statements to the correct speaker.',
        'Summarization and action item extraction via Groq-hosted LLM with structured JSON output for reliable downstream processing.',
        'Emotion/sentiment analysis via Hume AI adds qualitative context to the meeting record.',
        'All data persisted in Supabase; action items automatically pushed to Trello cards with correct labels and assignments.',
      ],
      architecture: [
        'UI → Flask REST API (central pipeline orchestrator)',
        'Flask → AssemblyAI: audio upload + async polling for transcript with speaker diarization',
        'Flask → Groq LLM: structured prompt requesting JSON-formatted summary and action item extraction',
        'Flask → Hume AI: emotion analysis on transcript segments, parallelized with Groq call post-transcript',
        'Flask → Supabase: store meeting record, full transcript, extracted action items, and emotion timeline',
        'Flask → Trello API: create labeled cards from extracted action items with speaker attribution',
        'Frontend: displays emotion timeline visualization, transcript with speaker labels, summary, and action item checklist',
      ],
      decisions: [
        'AssemblyAI over self-hosted Whisper: managed API with speaker diarization built-in — no infra to manage, faster iteration during a 24-hour hackathon sprint',
        'Groq over OpenAI: significantly lower latency for LLM calls — critical for a demo where every second of wait time hurts the impression',
        'Structured JSON output from LLM: forces action items into a predictable schema, makes Trello sync deterministic and reliable',
        'Supabase over raw Postgres: instant REST + auth APIs with zero backend setup for the storage layer — right call for time-boxed builds',
        'Parallelized Hume + Groq calls: once transcript is ready, both emotion analysis and summarization run concurrently, cutting end-to-end latency roughly in half',
      ],
      impact: [
        'Hackathon Runner-Up recognition from judges',
        'End-to-end: audio → full insights in ~TBD seconds for a 10-minute meeting recording',
        'Eliminated manual note-taking and task creation overhead in the demo scenario',
        'Action items surface in Trello automatically — demonstrated a complete automation loop',
      ],
      challenges: [
        'Speaker diarization accuracy: AssemblyAI occasionally misattributes short utterances — partially mitigated by post-processing to merge adjacent same-speaker segments',
        'LLM output consistency: occasional format deviation from structured JSON schema — handled with strict schema validation and automatic retry with a stricter prompt on parse failure',
        'Rate limits: Hume API has strict per-minute limits — implemented exponential backoff with jitter and request batching to stay within quotas',
        'End-to-end latency: chaining multiple async APIs accumulates wait time — optimized by parallelizing independent calls and using streaming where APIs supported it',
      ],
      nextSteps: [
        'Real-time streaming transcription for live in-meeting assistance instead of post-processing only',
        'Improved speaker diarization with a fine-tuned identification model',
        'Calendar integrations (Google Calendar, Outlook) to auto-fetch meeting context and participants',
        'Robust OAuth authentication for multi-user deployment beyond demo scale',
        'Additional integrations: Notion, Linear, Jira alongside Trello',
      ],
    },
  },
  {
    slug: 'brain-tumor-yolov8',
    title: 'Brain Tumor Detection',
    tagline: 'End-to-end tumor detection pipeline using YOLOv8 with a Streamlit UI and Dockerized deployment — backed by two IEEE publications.',
    category: 'Machine Learning / Applied CV',
    featured: false,
    tech: ['YOLOv8', 'YOLOv5', 'Python', 'Streamlit', 'Docker', 'OpenCV', 'PyTorch', 'GitHub Actions'],
    highlights: [
      'Fine-tuned YOLOv8 on annotated MRI dataset — mAP@50: TBD',
      'Published two IEEE papers on brain tumor detection (YOLOv5 + SVM approaches)',
      'Streamlit inference UI + Dockerized pipeline for reproducible, accessible demos',
    ],
    links: {
      github: 'https://github.com/Mithileshan/brain-tumor-yolov8',
    },
    caseStudy: {
      problem: [
        'Manual detection of brain tumors from MRI scans is time-intensive and requires specialist expertise.',
        'Detection models must localize tumor regions precisely, not just classify the image — bounding boxes matter.',
        'ML model demos are often locked in Jupyter notebooks, inaccessible to non-technical stakeholders who need to evaluate them.',
      ],
      solution: [
        'Fine-tuned YOLOv8 on an annotated brain MRI dataset to detect and localize tumor regions with bounding boxes and confidence scores.',
        'Built a Streamlit web app for interactive inference: upload an image and get annotated output instantly without any code.',
        'Dockerized the inference stack for consistent, reproducible environments across different machines and OSes.',
        'Added a CI pipeline via GitHub Actions to automate environment validation and linting on every push.',
      ],
      architecture: [
        'Training pipeline: offline YOLOv8 fine-tuning with data augmentation (flip, rotation, brightness jitter, mosaic)',
        'Model artifacts: best.pt checkpoint stored for versioning and reproducible inference',
        'Inference pipeline: Streamlit app loads model at startup, runs inference on user-uploaded images',
        'Output: annotated image with bounding boxes, class labels, and per-detection confidence scores',
        'Docker container: packages model weights + Streamlit app for a consistent portable runtime',
        'CI: GitHub Actions validates environment, checks formatting, and confirms the model loads on every push',
      ],
      decisions: [
        'YOLOv8 over Faster R-CNN: faster inference (real-time capable on CPU), simpler fine-tuning pipeline, strong Ultralytics community support',
        'Streamlit over Flask + custom frontend: dramatically reduces UI development time for a demo tool — right tradeoff for a research showcase',
        'Docker for inference: eliminates environment mismatch issues, makes the demo trivially reproducible by anyone with Docker',
        'Transfer learning from COCO-pretrained weights: accelerates convergence and reduces overfitting risk, especially critical with a limited medical dataset',
        'GitHub Actions CI: catches broken environments before they reach main, enforces discipline even on solo projects',
      ],
      impact: [
        'IEEE Published: "Detection of Brain Tumor Using YOLOv8 Algorithm" (Mar 2024)',
        'IEEE Published: "Brain Tumor Detector Using SVM Algorithm" (Feb 2023)',
        'mAP@50: TBD',
        'mAP@50-95: TBD',
        'Precision: TBD | Recall: TBD | CPU inference latency: TBD ms',
      ],
      challenges: [
        'Class imbalance: more normal/negative samples than tumor positives — addressed with weighted loss function and aggressive augmentation on the minority class',
        'Small tumor detection: small regions missed at standard input resolution — addressed by increasing training image size and using a higher-resolution inference pass',
        'Label quality: inconsistent annotation boundaries in the source dataset — filtered outliers with IoU-based label consistency checks',
        'Overfitting on limited data: controlled via early stopping on validation mAP, dropout, and mosaic augmentation to increase effective dataset diversity',
      ],
      nextSteps: [
        'Export model to ONNX for faster CPU inference and broader runtime compatibility',
        'INT8 quantization for edge deployment without significant accuracy loss',
        'Add GPU inference support in the Docker container with CUDA base image',
        'Expand training data by incorporating additional public MRI datasets',
        'Implement k-fold cross-validation for a more rigorous accuracy estimate',
        'Add a confidence threshold slider in the Streamlit UI for real-time sensitivity control',
      ],
    },
  },
  {
    slug: 'devtracker',
    title: 'DevTracker',
    tagline: 'Multi-tenant bug tracking SaaS with JWT auth, Redis caching, role-based access control, and Swagger-documented APIs.',
    category: 'Full-Stack / SaaS',
    featured: false,
    tech: ['React', 'Node.js', 'Express', 'PostgreSQL', 'Redis', 'JWT', 'RBAC', 'Swagger', 'Docker'],
    highlights: [
      'Redis caching reduced API calls by 60%, maintaining sub-100ms response times across 1,000+ records',
      'Multi-tenant data isolation with role-based access (Admin / Manager / Developer)',
      'JWT + refresh token auth, audit logging, and Swagger docs baked in from day one',
    ],
    links: {
      github: 'https://github.com/Mithileshan/devtracker',
    },
    caseStudy: {
      problem: [
        'Engineering teams need a structured workflow to create, assign, track, and close issues without losing context.',
        'In multi-team SaaS environments, proper tenant isolation is critical — one team must never access another\'s data.',
        'Ad-hoc tools like spreadsheets and chat don\'t enforce accountability or provide auditable trails.',
      ],
      solution: [
        'Built a SaaS-style bug tracker with full tenant isolation, role-based access control, and a complete issue lifecycle.',
        'Designed a REST API with Swagger/OpenAPI documentation to make the system maintainable and integrable.',
        'Implemented JWT-based authentication with refresh token rotation for secure, long-lived sessions.',
        'Added audit logs that capture all sensitive operations: assignment changes, status transitions, role modifications.',
      ],
      architecture: [
        'Frontend: React SPA with component-based design and client-side routing',
        'Backend: Node.js + Express REST API with layered architecture (routes → controllers → services → repositories)',
        'Database: PostgreSQL with tenant_id column strategy for multi-tenant data isolation',
        'Auth: JWT access tokens (short-lived) + refresh tokens (rotated on use, stored securely)',
        'Docs: Swagger/OpenAPI spec with auto-generation from route definitions',
        'Deployment: Docker + Docker Compose for reproducible local and production environments',
      ],
      decisions: [
        'Single-DB multi-tenancy (tenant_id column) over separate schemas — simpler to maintain at this scale, easier migrations, still provides logical data isolation',
        'JWT + refresh token rotation over server sessions — stateless auth scales horizontally without shared session storage',
        'Composite indexes on (tenant_id, status, created_at) to make the most common issue list queries fast at scale',
        'Soft deletes over hard deletes — preserves audit history and allows data recovery without complex backup workflows',
        'Swagger-first API design — documentation is part of the development contract, not an afterthought that gets skipped',
      ],
      impact: [
        'Redis caching reduced API calls by 60%, maintaining sub-100ms response times across 1,000+ records',
        'PostgreSQL composite indexing on (tenant_id, status, created_at) eliminated full-table scans on high-traffic queries',
        'Swagger/OpenAPI docs reduced developer onboarding friction — all endpoints self-documented',
        'Tenant-scoped uniqueness constraints eliminated duplicate issue reports across teams',
      ],
      challenges: [
        'Multi-tenant query safety: ensuring every data query is always scoped to the correct tenant_id — solved with a request middleware that injects and validates tenant context before any DB operation',
        'Refresh token security: preventing token reuse and replay attacks — solved with rotation on use + immediate invalidation when reuse is detected',
        'Role hierarchy enforcement: expressing who can do what without a maze of if-else checks — solved with a declarative permissions table and a reusable authorization middleware guard',
      ],
      nextSteps: [
        'Rate limiting per tenant to prevent API abuse',
        'SSO / OAuth integration (Google Workspace, GitHub)',
        'Email notifications for assignments and status transitions',
        'Event queue (e.g., BullMQ) for async side effects — notifications, webhooks, integrations',
        'Full observability: distributed tracing, structured logs, Grafana dashboard',
      ],
    },
  },
  {
    slug: 'tinker',
    title: 'Tinker',
    tagline: 'RAG-based document Q&A platform — upload any document and get instant LLM-powered summaries and context-aware answers.',
    category: 'AI Product / Hackathon',
    featured: false,
    tech: ['Python', 'LLM', 'NLP', 'RAG', 'Flask', 'Streamlit'],
    highlights: [
      'Document ingestion pipeline — parses and chunks documents for context-aware Q&A',
      'LLM-powered summarization and question answering grounded in uploaded document content',
    ],
    links: {
      github: 'https://github.com/Mithileshan/Document-Summarizer',
    },
  },
]
